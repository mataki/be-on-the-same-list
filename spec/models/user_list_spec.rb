require 'spec_helper'

describe UserList do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UserList.create!(@valid_attributes)
  end
end
